{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "project-mcp-tools": {
      "type": "local",
      "command": [
        "uv",
        "--directory", "../project-mcp-tools",
        "run", "mcp-server",
        "--target-project", "../super-fruit-world"
      ]
    }
  }
}
